PORT=4000
CORS_ORIGIN=*

# Password required to access /admin/* routes and the solo test mode.
# Copy this file to .env (gitignored) and set your own value — never commit
# the real password.
ADMIN_PASSWORD=changeme
